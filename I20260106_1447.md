 Perfect! I've added git tag support to the build info. Here's what's new:

  ✅ Updated Features

  1. Version Display (Git Tag)

  The build info now shows the last git tag as the version:

  ──────── Status ────────
  Version:      v1.0.2          ← NEW!
  Build Time:   2026-01-06 13:45 UTC
  Commit:       a586c94
  Message:      feat: add build version info...
  Branch:       main
  Environment:  staging

  2. Smart Fallback

  Profile Screen Tile:
  - Shows tag (e.g., v1.0.2) if tags exist
  - Falls back to commit hash (e.g., a586c94) if no tags

  3. Updated Script Output

  ./scripts/generate_build_info.sh

  ✓ Build info generated
    Version:     v1.0.2         ← NEW!
    Build Time:  2026-01-06 13:45 UTC
    Commit:      a586c94
    Branch:      main
    Environment: staging

  📋 How It Works

  The script uses git describe --tags --abbrev=0 to get the most recent tag:

  # Returns the last tag (e.g., v1.0.2)
  git describe --tags --abbrev=0

  No tags? Shows "no-tag" and falls back to commit hash in UI.

  🏷️ Tagging Workflow

  To create version tags:

  # Create a new version tag
  git tag v1.0.3

  # Push tags to remote
  git push --tags

  # Regenerate build info
  ./scripts/generate_build_info.sh

  🎯 Updated Display

  Profile Screen:
  - Shows v1.0.2 in the trailing text (if tagged)
  - Shows a586c94 if no tag exists
  - Monospace font for professional look

  Build Dialog:
  - Full version: v1.0.2 (a586c94)
  - Complete build metadata with version at the top

  The version info is now production-ready with proper semantic versioning support!
