 ✅ Fixed: Google OAuth and iOS Build Issues

  Changes Made

  1. Updated Google OAuth to use correct backend mutation:
  - ✅ Changed from signInWithGoogle → exchangeGoogleIdToken
  - ✅ Updated input type: ExchangeGoogleIdTokenInput
  - ✅ Handle string response (accessToken) instead of object
  - ✅ Use Google account data from SDK
  - ✅ Updated GraphQL service whitelist

  2. Fixed iOS Swift linker error:
  - ✅ Added IPHONEOS_DEPLOYMENT_TARGET = '16.0' to post_install block
  - ✅ Cleaned and reinstalled pods
  - ✅ Cleaned Flutter build

  How Google OAuth Works Now

  1. User taps "Sign in with Google"
  2. Google Sign-In SDK authenticates → returns idToken
  3. App calls: exchangeGoogleIdToken(input: { idToken })
  4. Backend validates idToken → returns accessToken string
  5. App stores accessToken + AUTH_CODE
  6. GraphQL client refreshes with new token
  7. User navigates to home screen

  Next Steps

  Xcode is opening - Run the app from there:
  1. Select your iPhone device
  2. Press ⌘R to run
  3. Test Google Sign-In

  The app should now:
  - ✅ Build successfully (Swift error fixed)
  - ✅ Connect to correct backend mutation
  - ✅ Complete Google OAuth flow end-to-end

  Commit ready (not pushed yet):
  fix: update Google OAuth to use exchangeGoogleIdToken mutation

  Test Google login and let me know if it works! 🚀
